// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`. (Note: We'll use environment.development.ts)
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  apiUrl: 'https://your-production-api-domain.com/api/v1', // Placeholder for production v1 API
  googleMapsApiKey: 'YOUR_PRODUCTION_API_KEY', // Placeholder - Replace with actual production key if different
};
